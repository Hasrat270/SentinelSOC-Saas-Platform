{
  "name": "sentinelsoc-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm run build --prefix frontend",
    "install": "npm install --prefix frontend",
    "start": "npm start --prefix frontend"
  }
}
